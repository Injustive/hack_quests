import os

from utils.run_config import ROOT_DIR


REFERRALS = os.path.join(ROOT_DIR, 'run_hack_quests', 'data', 'referrals.txt')
UNUSED_REFERRALS = os.path.join(ROOT_DIR, 'run_hack_quests', 'data', 'unused_referrals.txt')
